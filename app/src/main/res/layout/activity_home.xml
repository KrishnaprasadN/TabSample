<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="template.app.com.tabsample.HomeActivity">

        <Button
            android:id="@+id/jw_title"
            android:layout_width="fill_parent"
            android:layout_height="70dp"
            android:text="JakeWharton TitlePagerIndicator" />

        <Button
            android:id="@+id/tabs"
            android:layout_width="fill_parent"
            android:layout_height="70dp"
            android:layout_below="@id/jw_title"
            android:text="Support PagerTabStrip" />

        <Button
            android:id="@+id/sliding_tabs"
            android:layout_width="fill_parent"
            android:layout_height="70dp"
            android:layout_below="@id/tabs"
            android:text="Google SlidingTabLayout + SlidingTabStrip" />

        <Button
            android:id="@+id/astuets_tabs"
            android:layout_width="fill_parent"
            android:layout_height="70dp"
            android:layout_below="@id/sliding_tabs"
            android:text="Astuetz Tab Library" />

        <Button
            android:id="@+id/smart_tab_layout"
            android:layout_width="fill_parent"
            android:layout_height="70dp"
            android:layout_below="@id/astuets_tabs"
            android:text="Smart Tab Layout Library" />


</RelativeLayout>
